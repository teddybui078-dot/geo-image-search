import Foundation

// Next Step 2 — SQLite metadata table (asset identity, GPS, timestamp, geocoded place),
// SQLite R-Tree module for geospatial range queries, sqlite-vec for embedding similarity.
enum Schema {
    static let placeholder = "See DESIGN.md Architecture Decisions for the schema this backs."
}
