import Foundation

// Next Step 4 — LOD/clustering via the chosen globe library's built-in support
// (CesiumJS or OpenGlobus — library choice still an Open Question in DESIGN.md).
enum PinClustering {}
